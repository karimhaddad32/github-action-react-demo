name: Test Project
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get/Clone code
      #uses is for using actions
        uses: actions/checkout@v4          
      - name: Setup NodeJS
        uses: actions/setup-node@v4.2.0
        with: 
          node-version: 18
      - name: Install Dependencies
        run: npm ci
      - name: Run Tests
        run: npm test



